var os = require('os');

const cpu = async (client, message, args, name, code) => {

    let r = ((os.freemem()) * 100 / os.totalmem()).toFixed(2)

    code = code.replaceLast("$cpu", r + "%")

    return {
        code: code
    }
}

module.exports = cpu