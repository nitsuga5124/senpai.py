const Discord = require("discord.js") 
const authorAvatar = (client, message, args, name, code) => {
  
  code = code.replaceLast("$authorAvatar", message.author.displayAvatarURL()) 
     // text = text.replace("$authorID", message.auth
  return {
    code: code,
  } 
}

module.exports = authorAvatar;